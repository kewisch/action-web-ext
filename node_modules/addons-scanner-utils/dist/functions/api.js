"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.patchScannerResult = void 0;
const error_1 = require("./error");
const auth_1 = require("./auth");
/**
 * PATCH a scanner result to the AMO API, authenticating with a JWT token
 * computed from the `AMO_JWT_ISS_KEY` and `AMO_JWT_SECRET` environment
 * variables.
 *
 * @throws AMOError if the response is not ok
 */
const patchScannerResult = (_a) => __awaiter(void 0, [_a], void 0, function* ({ url, payload, env, }) {
    const response = yield fetch(url, {
        method: 'PATCH',
        headers: {
            'Content-Type': 'application/json',
            Authorization: `JWT ${(0, auth_1.makeJWT)({ env })}`,
        },
        body: JSON.stringify(payload),
    });
    if (!response.ok) {
        throw new error_1.AMOError(`unexpected response: ${response.statusText}`);
    }
});
exports.patchScannerResult = patchScannerResult;
