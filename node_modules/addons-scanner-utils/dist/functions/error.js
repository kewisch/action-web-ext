"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AMOError = exports.createAppError = void 0;
/**
 * Create an app error object with status code and extra information.
 */
const createAppError = ({ message, extraInfo, status = 500, }) => {
    const error = new Error(message);
    error.status = status;
    error.extraInfo = extraInfo;
    return error;
};
exports.createAppError = createAppError;
/**
 * Error object used when interactions with the AMO API led to errors.
 */
class AMOError extends Error {
    constructor(message) {
        super(message);
        this.name = 'AMOError';
    }
}
exports.AMOError = AMOError;
